spring.application.name=Task_Management
# Database Configuration
spring.datasource.url=jdbc:mysql://mysql:3306/task_management?useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true&connectionTimeout=30000
spring.datasource.username=root
spring.datasource.password=Hhdh573)&@/dg
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
# For development (auto-creates tables):
spring.jpa.hibernate.ddl-auto=validate
# For production (with schema.sql):
# spring.jpa.hibernate.ddl-auto=validate
# spring.sql.init.mode=always

# Connection Pool
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=5

# Show SQL for debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
